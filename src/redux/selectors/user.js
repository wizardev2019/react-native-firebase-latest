// import { get } from 'lodash';

// export const getCredentials = state => get(state, 'userReducer.credentials', []);
// export const getDatapoints = state => get(state, 'userReducer.datapoints', []);

// export const getCredentialByType = (state, type) => {
//   const credentials = getCredentials(state);
//   return credentials.find(credential => credential.type[1] === type);
// };

// export const getDatapointByType = (state, type) => {
//   const datapoints = getDatapoints(state);
//   return datapoints.find(datapoint => datapoint.DataPoint === type);
// };
